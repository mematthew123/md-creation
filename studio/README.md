# md-studio

Sanity Studio for md-creation (project `5ouc347b`, dataset `production`).

Run from the monorepo root:

```bash
npm run dev:studio
```

Or from this directory: `npm run dev`. Other scripts: `build`, `deploy`, `lint`, `typecheck`.

Schema types live in `schemaTypes/`.
